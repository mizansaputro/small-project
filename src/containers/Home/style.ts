import { SxProps, Theme } from "@mui/material";


export const homeBoxContainer = {
    alignItems: "center",
    backgroundColor: "blue.100",
    display: "flex",
    justifyContent: "center",
    minHeight: "100vh",
} as SxProps<Theme>;

export const homePaperListContainer = {
    borderRadius: 1,
    padding: { xs: 2, md: 4 },
} as SxProps<Theme>;
